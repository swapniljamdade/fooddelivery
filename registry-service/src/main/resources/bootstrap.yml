spring:
  application:
    name: registry-service
  cloud:
    config:
      uri: http://127.0.0.1:8888
      
server:
   port: 8761

eureka:
  client:
     registerWithEureka: false
     fetchRegistry: false
  instance:
     preferIpAddress: true